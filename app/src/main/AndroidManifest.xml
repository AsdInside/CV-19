<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.e.cv_19">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-feature android:name="android.hardware.location.gps" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_round"
        android:supportsRtl="true"

        android:theme="@style/AppTheme">

        <activity android:name=".Activities.Activity_menu"/>
        <activity android:name=".Activities.Activity_richiesta_cancellazione" />
        <activity android:name=".Activities.Activity_visualizza_recensioni_struttura" />
        <activity android:name=".Activities.Activity_ricerca" />
        <activity android:name=".Activities.Activity_risultati_ricerca" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:windowSoftInputMode="adjustResize|stateHidden"
            android:name=".Activities.Activity_visualizza_mappa"
            android:label="@string/title_activity_visualizza_mappa" />

        <activity android:name=".Activities.Activity_mostra_struttura" />
        <activity android:name=".Activities.Main_Activity" />
        <activity android:name=".Activities.Activity_modifica_impostazioni" />
        <activity android:name=".Activities.Activity_recupera_password" />
        <activity android:name=".Activities.Activity_visualizza_recensioni" />
        <activity android:name=".Activities.Activity_registrazione" />
        <activity android:name=".Activities.Activity_login">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>